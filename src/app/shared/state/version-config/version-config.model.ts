import * as dom from "@codiac.io/codiac-domain"

export interface VersionConfig extends dom.VersionConfig {
}

export function createVersionConfig(params: Partial<VersionConfig>) {
  return {

  } as VersionConfig;
}
